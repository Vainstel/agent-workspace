---
description: Activate the workspace-manager role — set up / manage the workspace structure interactively
argument-hint: what to do (optional — otherwise starts with discovery)
---

Read `.claude/agents/workspace-manager.md` now. Adopt everything after its frontmatter as YOUR role for this session — you (the main agent) run its flows directly and interactively; do not spawn it as a subagent. Follow its non-negotiables, session-start discovery, and flows exactly.

If a task is given below, handle it per the role; otherwise start with the session-start discovery and offer the user the relevant options.

$ARGUMENTS
