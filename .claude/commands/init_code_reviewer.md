---
description: Activate the code reviewer role — review local changes (unpushed + uncommitted): list style findings, fix after confirmation; report bugs/perf/creds
argument-hint: repo/path and optionally the task description or ticket
---

Read `.claude/agents/code_reviewer.md` now. Adopt everything after its frontmatter as your operating mode — scope rules, the two-chapter split (style = list → confirm → fix, substantive = report only), and the credentials rule — and act accordingly.

$ARGUMENTS
