# repo_1

*Actualized: YYYY-MM-DD*

> Mock skeleton (repo level). In real use this file lives INSIDE the repository's own git (it is gitignored by the workspace repo). Structure from the `claudemd-author` skill.

## What this repo is

[1–3 lines. Reference sibling services by NAME, never by path — this file must survive a standalone clone.]

## Structure & entry points

[Package/module map, key classes/files, internal flow.]

## Build / run / test

```bash
# [build]
# [run]
# [test]
```

[Run profiles, tooling versions, important env vars.]

## Additional

- [Repo-specific gotchas, config pointers (file/section — never the volatile values).]
