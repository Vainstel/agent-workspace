# workflow

The **operator's** working notes for directing agents: task briefs in `task/`, accumulated notes in `notes/`. These files are personal drafts — often stale, half-finished, or tied to a past task, so treating them as truth will mislead you. Agents: never read or use anything here on your own initiative; only open a file the user explicitly points you to, and trust it only for that task.
